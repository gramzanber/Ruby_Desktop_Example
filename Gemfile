source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '3.1.0'    # Update this to reflect the version of Ruby you are developing with
gem 'tk'
gem 'openssl'
gem 'base64'